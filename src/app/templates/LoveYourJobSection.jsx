import { gridConfig } from '@/app/utils/styling'

const LoveYourJobSection = () => {
    return ( 
        <section className={`h-screen w-full bg-neutral-1-100`}>
            <div className={`${gridConfig} w-full h-full`}>
            
            </div>
        </section>
     );
}
 
export default LoveYourJobSection;